'use client'

import { Popover as PopoverBase } from '@headlessui/react'

const Popover = PopoverBase
const PopoverButton = PopoverBase.Button
const PopoverPanel = PopoverBase.Panel
const PopoverGroup = PopoverBase.Group
const PopoverOverlay = PopoverBase.Overlay

export { Popover, PopoverButton, PopoverPanel, PopoverGroup, PopoverOverlay }
